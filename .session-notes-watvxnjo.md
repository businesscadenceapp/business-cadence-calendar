# Session Notes (internal, this session only — do not ship)

## Fixed already
- tsconfig.json: removed `"extends": "expo/tsconfig.base"` (expo pkg not installed → broke tsx path aliases → ERR_MODULE_NOT_FOUND @shared/const). Also excluded client/src/expo-entry.tsx (imports react-native, not installed). Dev server now runs on port 3000. TS: 0 errors.

## Auth for screenshots
- App auth is localStorage-based (no cookies): keys `bcc_auth_v1`="granted", `bcc_person_v1`=JSON person, `bcc_account_id`, `bcc_active_business_slug`, `bcc_active_business_id`, `bcc_active_business`.
- Test person: id=99bckXJ69bq9sCRf, email test@businesscadence.com, role owner, accountId=180006; business: id=60001 "bubbles Realty" slug=general.
- Real owners: wehctam@yahoo.com (owner, acct=3), bubblz2828@yahoo.com (coowner, acct=3). Business acct=0: "New Beginnings Chiropractic" slug=chiropractic (accountId mismatch: persons acct=3 but biz acct=0 — todo.md says Matt/Lynn updated 0→3, but biz row still acct=0!? board works via slug).
- IMPORTANT: webdev_take_screenshot uses a separate browser context WITHOUT localStorage → all /app/* screenshots show the login page. Must use browser tools (sandbox browser with console-set localStorage + device emulation) OR temporarily seed localStorage via a query param/dev hook. Best approach: use browser_navigate with mobile viewport? Browser tool viewport is desktop. Alternative: add a dev-only URL param handler? Simpler: audit mobile CSS via code reading + use browser at narrow window via console (window.resizeTo doesn't work). Consider Playwright script in sandbox with localStorage init + 375x812 viewport for screenshots.

## Capacitor facts
- capacitor.config.json: appId com.businesscadence.calendar, webDir dist/public, splash bg #0A1929, StatusBar dark.
- ios/ folder exists (Xcode Cloud build w/ ci_scripts). NO android/ folder yet despite todo claims.
- package.json has cap:sync etc. Expo leftovers: app.json, eas.json, expo.vite.config.ts, client/src/expo-entry.tsx, scripts expo:*, devDeps @expo/ngrok, eas-cli, expo-cli (unused; expo path abandoned in favor of Capacitor).
- Brand assets: /manus-storage/businesscadence-note-teal_5fad453b.png (teal note icon), /manus-storage/businesscadence-logo-final-clean_3f67cebb.webp (wordmark). Theme: dark navy #0A1929 sidebar, #0F2440 main bg, teal #5EEAD4 accent.

## Dev preview URL
- https://3000-ioh7klceb60tsurt8hirl-3bbc26d5.us2.manus.computer

## Mobile audit findings (375x812, via /home/ubuntu/mobile_shots.py -> /home/ubuntu/shots/*.png)
- Mobile header (AppShell): business switcher shows "Chiropractic" — note it shows wrong biz name for acct 180006 session? Header Owner/Team pill is CRAMPED: "Owner" text overlaps the Team icon; needs spacing fix. Otherwise header OK.
- Board: looks good on mobile. Card grid 2x2 fine. FAB fine.
- Goals: header row (title + year select + Add Goal) fits. Empty state fine. Looks good.
- KPI: looks good; cards stack vertically already.
- Note: the biz shows "Chiropractic" because localStorage slug "general"? No — shows chiropractic despite acct 180006/bubbles Realty. Investigate business resolution later if relevant (screenshot-only concern).
## Full audit results (all pages reviewed at 375x812)
- board: GOOD. goals: GOOD. kpi: GOOD. reports: GOOD (tab icons row + segmented control fit). calendar: GOOD (summary cards 2-col, calendar grid fits). settings: GOOD. checkin: GOOD. team: GOOD.
- ONLY consistent issue: AppShell mobile header Owner/Team segmented pill is cramped — "Owner" label text overlaps/touches the Team icon; pill needs more padding/gap. Everything else renders cleanly.
- Conclusion: prior sessions already did most mobile optimization. Remaining: (1) header pill spacing fix; (2) check deeper interactive views (board card detail, meeting detail, modals) quickly; (3) then focus on Capacitor build prep (bigger win).
- Domains now bound: businesscadence.com, www.businesscadence.com, bizcalendars-my5dqbgh.manus.space.

## Fixes applied this session
- AppShell mobile Owner/Team pill: icon-only below 390px (labels shown ≥390px), better padding; verified at 375px — clean segmented control now.

## Interactive views audit (375x812)
- ix_board_tasks: GOOD — back button, empty state, FAB all clean.
- ix_board_compose: GOOD — bottom-sheet compose with drag handle, full-width post-type buttons, date, textarea, attach, notify hint, Post button. Touch targets generous. Notify picker EXISTS (says "leave blank to notify owners only").
- ix_calendar_day: GOOD — day tap opens full-screen scheduled-for panel with meeting card, agenda checklist; touch-friendly.
- Remaining to check: ix_goals_bottom (bottom nav/safe area), ix_team_full.
- NOTE: mobile header pill verified icon-only at 375px. Bottom tab bar visibility needs check — goals_bottom shot.
- RESOLVED: DOM check confirms bottom tab bar IS rendered on all pages (fixed, bottom:0, top:756/812, h:56, z:40, correct items: owner nav = Board/Goals/KPIs/Reports/More; team nav = My Board/Schedule/KPIs/Check-in/More). It was merely covered in screenshots by the dev-only "page is not live" Manus banner (~90px, bottom). Production unaffected. Safe-area insets present via env() on header, main padding, and tab bar.
- Meeting detail verified: the day-tap panel IS the full meeting detail (agenda checkboxes, per-item context inputs, all-business items, meeting notes textarea) — scrolled to bottom, everything full-width, touch-friendly, no overflow at 375px. Close (×) present at top. Mobile pass COMPLETE.

## Next: Capacitor build prep (phase 4-5)
- Need 1024 app icon + splash. Brand: teal music-note? No — brand icon is a NOTE/checkmark logo: /manus-storage/businesscadence-note-teal_5fad453b.png (used as BrandIcon). Colors: navy #0A1929 bg, teal #5EEAD4.
- capacitor.config.json exists; ios/ exists (Xcode Cloud ci_scripts); NO android platform folder yet.
- Plan: check package.json cap scripts + installed @capacitor pkgs; generate icon/splash via imagegen; use @capacitor/assets to emit native asset sets; add android platform; cap sync; verify build; checkpoint.

## Capacitor state (verified)
- Installed: @capacitor/{android,app,cli,core,device,haptics,ios,keyboard,splash-screen,status-bar} v8, capacitor-voice-recorder 7.0.6 (installed but NOT used in code — RecordMeeting.tsx still uses web MediaRecorder/getUserMedia).
- Scripts: cap:sync = pnpm build && npx cap sync; cap:open/run:ios/android.
- iOS assets EXIST but are PLACEHOLDER/default Capacitor blue-X logo: AppIcon-512@2x.png 1024px (generic blue X on white grid) + Splash 2732 (tiny blue X centered on white). NOT brand assets — need replacement with BusinessCadence branding (navy #0A1929 bg, teal #5EEAD4, note logo /manus-storage/businesscadence-note-teal_5fad453b.png).
- android/ platform folder DOES NOT exist yet (only ios/). @capacitor/android installed, so `npx cap add android` should work.
- User knowledge: AVOID AI-generated images; prefer existing assets. So build icon/splash programmatically (PIL) from the existing brand logo PNG rather than AI imagegen.
- Brand logo source: need local copy — fetch from dev server /manus-storage/businesscadence-note-teal_5fad453b.png or check webdev-static-assets (empty dir listing earlier).

## Capacitor progress (done)
- Downloaded brand note PNGs via businesscadence.com (curl -L to follow CDN redirect) to /home/ubuntu/brand/.
- Composed assets programmatically with PIL (make_assets.py): icon-1024 (navy gradient + teal glow circle + note), icon-foreground/background (Android adaptive), splash-2732 + dark (badge + BusinessCadence wordmark + tagline). Previewed — looks on-brand.
- Copied into project assets/ dir; ran `npx @capacitor/assets generate --ios` then `--android` with #0A1929 backgrounds. iOS: 7 files into Assets.xcassets (new Default@Nx~universal~anyany[-dark].png, Contents.json updated; removed stale splash-2732x2732*.png). Android: 74 files (adaptive icons + splash densities).
- `npx cap add android` succeeded (android/ now exists). `pnpm build` OK. `npx cap sync` OK for both platforms; 7 plugins each.
- Known warn: capacitor-voice-recorder has no Package.swift (not SPM-compatible) — fine, project can use CocoaPods/It's listed in Podfile? ios/ uses CapApp-SPM… need to verify this plugin actually links on iOS build. NOTE for delivery: mention to user.
- Remaining: RecordMeeting.tsx native recording via capacitor-voice-recorder; vitest; typecheck; checkpoint.

## RecordMeeting native done + permissions
- Added client/src/hooks/useAudioRecorder.ts: Capacitor.isNativePlatform() branch — native uses capacitor-voice-recorder (dynamic import, requestAudioRecordingPermission, start/stop, base64+mime aac), web keeps MediaRecorder webm/opus; 16MB shared cap. RecordMeeting.tsx rewired; typecheck + 10 vitest pass.
- AndroidManifest: added RECORD_AUDIO + MODIFY_AUDIO_SETTINGS. iOS Info.plist: added NSMicrophoneUsageDescription.
- OPEN ISSUE: capacitor-voice-recorder 7.0.6 (latest) has no Package.swift → not SPM-compatible; project uses CapApp-SPM. Cap sync warns and does NOT add it to Package.swift. Options: (a) manually add .package to CapApp-SPM/Package.swift is impossible without Package.swift in plugin; (b) plugin ships CapacitorVoiceRecorder.podspec — but project is SPM-based, not Podfile; (c) vendored local Swift package wrapper pointing at plugin ios/Plugin sources. Investigate next.

## SPM fix implemented (vendored package)
- Created ios/VoiceRecorderSPM/Package.swift (swift-tools 5.9, iOS 15, depends capacitor-swift-pm 8.4.1) with target Sources/VoiceRecorderPlugin.
- scripts/sync-voice-recorder.mjs copies *.swift from node_modules plugin ios/Plugin + writes VoiceRecorderPluginRegistration.swift (extension VoiceRecorder: CAPBridgedPlugin with 8 CAPPluginMethods, replaces ObjC CAP_PLUGIN macro). Ran once; sources vendored.
- Registered in App.xcodeproj/project.pbxproj: PBXBuildFile 7A11AA01…, Frameworks phase entry, packageProductDependencies 7A11AA00…, packageReferences 7A11AA02… (XCLocalSwiftPackageReference relativePath ../VoiceRecorderSPM), plus XCLocal + product dependency definitions near end of file.
- package.json cap:sync now runs sync-voice-recorder.mjs between build and cap sync.
- pbxproj still needs validation (plutil not available on Linux; rely on Xcode Cloud/user build). Note: CAPBridgedPlugin conformance requires Capacitor >= 5 Swift plugin API — VoiceRecorder is ObjC-macro-registered CAPPlugin subclass; the extension approach mirrors how Capacitor 6+ SPM plugins register. If build errors occur user may need `identifier`/`jsName` adjusted — documented in delivery notes.
- package.json also has expo:* scripts + expo-cli/eas-cli devDeps from another session (Expo path abandoned in favor of Capacitor per todo history) — left untouched.
- FINAL registration style: class-level CAPPlugin, CAPBridgedPlugin with stored lets (matches official SplashScreenPlugin 8.x). sync script rewrites class decl on copy; exits 1 if upstream decl changes.
- Remaining before checkpoint: pnpm check + test + build; screenshot sanity of app pages; checkpoint (auto-publish note: web deploy unaffected by ios/android dirs).
