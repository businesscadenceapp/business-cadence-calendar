/**
 * Feature Flags
 *
 * Toggle features on/off for launch.
 * To enable Team features: set TEAM_ENABLED = true, rebuild, and redeploy.
 */

// Set to true when ready to launch the Team upgrade (owner → employee management)
export const TEAM_ENABLED = false;
